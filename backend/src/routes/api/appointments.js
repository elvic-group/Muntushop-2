/**
 * Appointments API Routes (Placeholder)
 */

const express = require('express');
const router = express.Router();

router.get('/', (req, res) => {
  res.json({ message: 'Appointments service - to be implemented' });
});

module.exports = router;

